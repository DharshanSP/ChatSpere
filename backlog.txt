ChatSphere — Backlog
=====================
Date: 13 June 2026

Done:
1. Created brand logo (CS) — SVG favicon + reusable Logo component
   - Applied to: index.html (favicon), Login, Register, ChatArea empty state, Sidebar header
2. Updated Vercel deployment config (vercel.json)
   - outputDirectory: client/dist
   - Only SPA rewrite, backend rewrites removed (env var approach instead)
3. Prepared backend for Railway deployment
   - Added railway.toml (build + start commands)
   - Server compiles and runs on Railway
4. Updated server CORS to accept multiple origins (comma-separated env var)
5. Updated frontend API & Socket services to use VITE_API_URL env var
   - api.ts: baseURL = ${VITE_API_URL}/api
   - socket.ts: connects to VITE_API_URL
6. Excluded test files from tsconfig to fix Vercel build failure

Pending / Known Issues:
1. VITE_API_URL env var needs to be set in Vercel project settings (currently missing https:// prefix)
   - Must be: https://chatspere-production.up.railway.app
   - Then redeploy Vercel
2. Backend (server/) is NOT yet deployed anywhere
   - Railway project exists (chatspere-production.up.railway.app) but needs env vars configured:
     * CORS_ORIGIN = https://chat-spere-ten.vercel.app
     * JWT_SECRET = <strong-random-string>
     * DATABASE_URL = file:chatsphere.db
3. Registration fails because frontend can't reach backend properly
   - Error: VITE_API_URL missing https:// → request goes to vercel.app instead of railway.app
4. Build note: pre-existing errors in client/src/test/components.test.tsx (unrelated to our changes)
